// Crear un vector vacio llamado "colores". Luego agrega 3 colores diferentes y recorre el vector

let colores = [];

colores.push("morado");
colores.push("verde");
colores.push("rojo");

for(let i = 0; i < colores.length; i++) {
    console.log(colores[i]);
}